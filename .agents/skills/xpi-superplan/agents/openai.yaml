interface:
  display_name: "Xpi Superplan"
  short_description: "Repo-specific patterns and workflows for xpi-superplan"
  default_prompt: "Use the xpi-superplan repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true